<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Modo botão flutuante: desenhar o botão sobre outros apps + serviço em
         primeiro plano que o hospeda (com notificação fixa). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notes">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Notes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auto captura em segundo plano: dá ao app o direito de ler o clipboard
             fora do primeiro plano. O usuário habilita em Ajustes → Acessibilidade. -->
        <service
            android:name=".capture.CaptureAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Detecta o fechamento do app (removido dos recentes) para desligar a
             auto captura — o estado não persiste com o app fechado. -->
        <service
            android:name=".capture.CaptureGuardService"
            android:exported="false" />

        <!-- Hospeda o botão flutuante (modo sem acessibilidade). -->
        <service
            android:name=".capture.FloatingButtonService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Botão flutuante para capturar texto copiado no bloco de notas do próprio usuário." />
        </service>

        <!-- Activity transparente e instantânea: ganha foco ao toque para ler o
             clipboard e captura; fecha na hora. -->
        <activity
            android:name=".capture.ClipboardReadActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Notes.Transparente" />

    </application>

</manifest>
